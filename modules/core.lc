import(operators)

## Arithmetic

external(+(a,b))
external(-(a,b))
external(*(a,b))
external(/(a,b))
external(%(a,b))

## Equality

external(==(a,b))
external(!=(a,b))

## Comparison

external(<(a,b))
external(>(a,b))
external(<=(a,b))
external(>=(a,b))

## Boolean

external(not(a))
external(and(a,b))
external(or(a,b))

## Branching

external(
    select(
        condition,
        if-true,
        if-false
    )
)